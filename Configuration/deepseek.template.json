{
  "base_url": "https://api.deepseek.com/v1",
  "model": "deepseek-flash",
  "thinking": {"type": "enabled"},
  "reasoning_effort": "max",
  "credential_reference": {
    "kind": "file",
    "path": "~/.kimi-code/config.toml",
    "toml_path": "providers.deepseek.api_key"
  },
  "notes": "GET /models serves deepseek-flash (the V4.1-Flash alias) and deepseek-v4-pro only; literal deepseek-v4.1 is rejected. Aliases observed in API responses, not immutable weights — the adapter verifies the configured model and records the echoed runtime model at session open. Requalify when provider mapping changes. Credential value never copied into this file."
}
