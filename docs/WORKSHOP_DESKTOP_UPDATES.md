# Desktop connection and work activity update

## Scope

This branch ports reviewed source changes onto the cleaned public history without importing prior development ancestry. It includes installed Electron lifecycle/package support, a per-call reconnecting MCP bridge, and an active-turn indicator with expandable durable activity history. Private task briefs, runtime data, credentials, logs, screenshots and unrelated project proposals are excluded.

The MCP bridge reconnects and authenticates for each new call; it never automatically replays a dispatched mutation. Installed Electron mode uses the bundled daemon and existing Workshop home, preserves a responding daemon, starts a missing daemon once with bounded readiness checks, and keeps UI exit separate from background work. Preview mode remains isolated.

## Activity semantics and data handling

The indicator requires a connected, fresh task read that reports working and nonempty runningEngineers. The label is Worker turn active; it does not prove token output, productive work or healthy native persistence. Polling is bounded to five seconds, freshness expires after fifteen seconds, request-start timestamps reject delayed state, and reduced-motion CSS stops animation. Waiting, blocked, terminal, disconnected or unknown states do not animate.

The existing outbox stores ordered task/turn/engineer activity records without a new database migration. Tool call/update correlation uses hashed opaque IDs; updates without starts remain valid observed events. Tool status, permission/auth/quota/uncertainty, message arrival and separately labeled lifecycle are preserved. Raw arguments/results, provider payloads, private prompts and reasoning are not added. Titles are bounded and redacted. Full committed messages remain in the conversation; the feed is not a full CLI transcript.

A task-scoped authenticated read API pages up to 200 records by sequence. The restricted preload exposes getActivity only. The collapsible feed deduplicates replay, isolates tasks, supports incremental history, and marks unavailable or pre-instrumentation history honestly. Lifecycle completion does not claim verification or infer missing tool results.

## Validation and release boundary

The source changes are under review and are not installed. Fresh Chrome validation passed at 1586×992 and 980×680 with exact inner/client/scroll measurements, screenshot inspection, peer selection, reset-after-create, legacy system-message display, activity expansion and zero normal-network console warnings/errors. The manifest binds the final renderer hashes; screenshots and evidence remain ignored local artifacts, so a fresh clone must regenerate them before packaging. A participation selector/toggle race discovered in Chrome was fixed.

Current checks: Swift 191 tests, 10 opt-in skips, zero failures; Web/Desktop 74 tests, 73 passed, one isolated-socket test skipped in the broad invocation and separately passed against a real isolated fake-adapter daemon; skill suite 9 passed; publication-guard suite 39 passed; Codex team-skill discovery passed without submitting work. The same MCP bridge survived daemon absence/start/stop/restart. Preview packaging and packaged Electron smoke passed, with Node/require absent from the renderer and exactly the restricted API exposed. The icon bytes and bundled daemon were verified. Do not count opt-in skips as live qualification.

Native remediation was separately exercised with existing authentication and the already-running relay, in disposable profiles/workspaces: native read of a synthetic input, explicit exact-command approval for `/usr/bin/true`, observed command completion, native session database creation/integrity, process close, session load in a new process, and session-only marker recall all passed. Native log checks found no database-open/autosave errors on the fixed source. Protected write/content-read/directory-listing regressions remain denied. This qualifies only that bounded native scenario, not arbitrary commands, all engineers, crash recovery, exclusive writer handover, private runtime inputs, or full installed-app operation.

Dependency audit remediation: the user independently applied the approved exact-version exception and installed `@xmldom/xmldom@0.9.12` on 2026-09-17. The reviewed package override pins that version, and the lockfile changes only its version, registry URL, integrity and removed deprecation field. The npm release-date cutoff and `ignore-scripts=true` remain set; this session did not modify those controls. Fresh `npm audit` reports zero vulnerabilities, and `npm ls` confirms a single valid 0.9.12 installation. Post-change verification passed: Web/Desktop 73 passed with the isolated-socket case separately passed, plist XML round-trip, preview packaging, packaged Electron smoke, icon and bundled-binary byte comparisons, and `git diff --check`. Preserve the approved lockfile when restoring dependencies; recreating it before that version clears the release-age rule may require policy-owner review. Source publication is covered by the user's separate instruction to commit and push after successful checks; installed-app replacement remains unauthorized.

Before an installed runtime update: finish supported web validation, bind final hashes, pass required tests, verify the bundle/icon and rollback backup, and ensure no active worker is interrupted. After installation, refresh the actual configured Codex MCP session and verify a fresh tool call plus current schema before end-to-end completion or task submission. Preserve the invocation/receipt journal. If no supported refresh works, record connection unverified and request the supported user action; do not kill Codex, retry unchanged failed probes or submit via another connection to conceal the missing check.

Permission decisions now use typed metadata, including the native inference-tool-name field, and correlate id-only requests with bounded per-session tool-call state. Titles and option labels cannot authorize commands; absent reject choices cancel rather than select an allow option, and only allow-once is selected. Exact command approvals default to an empty list in normal profiles. Normalized permission outcomes replace raw ACP/tool diagnostics. The approved sandbox change adds only literal metadata access to three profile ancestor directories so native SQLite can resolve its path; it grants neither directory contents nor writes.

No installed-profile migration, proposal promotion, permanent background service, main-branch merge or history rewrite is part of this change. Installed app qualification and the originating Codex connection remain unverified; source-only work does not prove or require refreshing that connection. The unrelated private project proposal and its separate acceptance defects remain outside this transfer.
