# Superseded exploratory source

This React/Vite scaffold was started before package-install approval was available. Installation failed/was rejected; it was not built or browser-qualified. The later user explicitly approved a dependency-free preview, which is implemented and validated in ../community-standalone/. Use that preview and its final design policy. This scaffold is retained as work history only; it is not the deliverable and must not be treated as the current design or runtime.
